import 'package:flutter/material.dart';

// 공동구매(교재) 페이지 -> 만든 파일 붙여넣기

class PurchaseScreen extends StatelessWidget {
  const PurchaseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
